#!/bin/sh

# dmenu prompt labeled with $1 to perform command $2.
# [ "$(printf "No\\nYes" | dmenu -i -p "$1" -nb darkred -sb red -sf white -nf gray )" = "Yes" ] && $2
[ "$(printf "No\\nYes" | dmenu -i -p "$1" -nb "#FFA800" -sb "#FF6A00" -sf white -nf black )" = "Yes" ] && $2
