## This file has been auto-generated by i3-config-wizard(1).
## It will not be overwritten, so edit it as you like.
##
## Should you change your keyboard layout some time, delete
## this file and re-run i3-config-wizard(1).
##

## i3 config file (v4)
##
## Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

## Font for window titles. Will also be used by the bar unless a different font
## is used in the bar {} block below.
font pango:monospace 8

## This font is widely installed, provides lots of unicode glyphs, right-to-left
## text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

## The combination of xss-lock, nm-applet and pactl is a popular choice, so
## they are included here as an example. Modify as you see fit.

## xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
## screen before suspend. Use loginctl lock-session to lock your screen.
#exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

## NetworkManager is the most popular way to manage wireless networks on Linux,
## and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

## set default layout
#workspace_layout tabbed

## Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

for_window [workspace="1"] floating enable
for_window [workspace="2"] floating enable
for_window [workspace="3"] floating enable
for_window [workspace="4"] floating enable
for_window [workspace="5"] floating enable
#for_window [workspace="4"] border none
#for_window [class="urxvt"] resize set 800 600
#for_window [class="URxvt"] border none
#for_window [class="URxvt"] border pixel 10
#for_window [class="URxvt"] client.focused #111111   #ff0000   #ffffff   #000000
#for_window [class="Alacritty"] border pixel 10
#for_window [class="Alacritty"] resize set 854 574
#for_window [class="Alacritty"] move position center
#bindsym $mod+KP_Add [class="Alacritty"] border pixel 10
#bindsym $mod+KP_Add [class="Alacritty"] urgent latest

no_focus [window_role="pop-up"]
popup_during_fullscreen smart

## Floating dialogs
#for_window [window_role="pop-up"] floating enable
#for_window [window_role="task_dialog"] floating enable

## Disable window titlebars entirely
default_border pixel 1
default_floating_border pixel 1
#new_window pixel 4       #deprecated in favor of the above options
#new_float pixel 4        #deprecated in favor of the above options
#for_window [class="^.*"] border pixel 1
#for_window [class="^.*"] border pixel 1

## gaps default inner>2 outer>2
gaps inner 0
gaps outer 0

## smart gaps on single window
smart_gaps on

## smart borders on|no_gaps
smart_borders on

## hide edge borders none|vertical|horizontal|both|smart  smart_no_gaps?
#hide_edge_borders both



exec_always --no-startup-id alttab -frame "#00cc00"
#exec_always --no-startup-id alttab -frame "#00cc00" -mk Alt_L -kk BackSpace
#exec_always --no-startup-id alttab -frame "#00cc00" -mk Super_L -kk BackSpace
#exec_always --no-startup-id ~/.config/i3/i3-focus-last.py
#exec_always --no-startup-id ~/.config/i3/i3-cycle-focus.py --history 3 --visible-workspaces --delay 0.1
#bindsym $mod+j                 exec ~/.config/i3/i3-focus-last.py --switch
#bindsym $mod+j                 exec ~/.config/i3/i3-cycle-focus.py --switch
#bindsym $mod+j                 exec ~/.config/i3/i3_cycle_windows
#bindsym $mod+j                 exec ~/.local/bin/i3-alttab-gist.py next
#bindsym $mod+k                 exec ~/.local/bin/i3-alttab-gist.py prev
#bindsym $mod+j                 exec ~/.config/i3/i3-alt-tab.py next current
#bindsym $mod+k                 exec ~/.config/i3/i3-alt-tab.py prev current


## Define names for default workspaces for which we configure key bindings later on.
## We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# set $i3l vstack 0.5 left to workspace $ws1
# set $i3l hstack 0.6 up to workspace $ws2
# set $i3l spiral 0.6 to workspace $ws3
# set $i3l 3columns 0.66 0.5 2 left to workspace $ws4
# set $i3l 2columns right to workspace $ws5
# set $i3l companion 0.3 0.4 up to workspace $ws6
# set $i3l autosplit to workspace $ws7

## switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

## move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

## change focus alternatively, you can use the cursor keys:
bindsym $mod+Left	  focus left
bindsym $mod+Down	  focus down
bindsym $mod+Up	    focus up
bindsym $mod+Right	focus right

## move focused window alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left  move left
bindsym $mod+Shift+Down  move down
bindsym $mod+Shift+Up	   move up
bindsym $mod+Shift+Right move right

## blue theme
# set $xforeground #c5c8c6
# set $xbackground #1d1f21
# set $xcursor #c5c8c6
# set $xcolor0 #0d3264
# set $xcolor8 #373b41
# set $xcolor1 #467ecb
# set $xcolor9 #cc6666
# set $xcolor2 #8c9440
# set $xcolor10 #b5bd68
# set $xcolor3 #de935f
# set $xcolor11 #f0c674
# set $xcolor4 #5f819d
# set $xcolor12 #81a2be
# set $xcolor5 #85678f
# set $xcolor13 #b294bb
# set $xcolor6 #6298e0
# set $xcolor14 #8abeb7
# set $xcolor7 #707880
# set $xcolor15 #c5c8c6

## Window Colors red/blue theme "just uncommented the theme"
# Class                 border          backgr      text            indic
#client.focused          $xcolor6       $xcolor1     $xbackground   $xcolor7
#client.focused_inactive $xcolor6       $xcolor0     $xcolor14
#client.unfocused        $xcolor6       $xcolor0     $xcolor14
#client.urgent           $xbackground   $xcolor9     $xbackground
#client.placeholder      $xcolor7       $xcolor7     $xcolor14

## Green_Theme
## class			            border		  backgr		  text		  indicator
#client.focused		      #111111     #00ff00     #ffffff		#000000
#client.focused		      #11111133   #00ff0033   #ffffff		#00000033
#client.focused		      #1111114d   #00ff004d   #ffffff		#0000004d
#client.focused		      #11111180   #00ff0080   #ffffff		#00000080
#client.focused		      #11111199   #00ff0099   #ffffff		#00000099
###############client.focused		       #11111166   #00ff0066   #ffffff		#00000066
###############client.focused_inactive #11111133   #06060633   #ffffff   #00000033
###############client.unfocused        #11111133   #06060633   #888888   #00000033
###############client.urgent           #ff0000     #ff0000     #ffffff   #000000
###############client.placeholder      #11111133   #0c0c0c33   #ffffff   #00000033
#client.background       #ffffff1a


## Gray_Theme
##class			            border		backgr		text		  indicator
client.focused		      #111111   #444444   #ffffff	 #000000
client.focused_inactive #111111   #060606   #ffffff   #000000
client.unfocused        #111111   #060606   #888888   #000000
client.urgent           #111111   #f00000   #ffffff   #000000
client.placeholder      #111111   #0c0c0c   #ffffff   #000000

## Dark_Theme
##class			            border		backgr		text		  indicator
#client.focused		       #111111   #000800   #ffffff	 #000000
#client.focused_inactive #111111   #191919   #ffffff   #000000
#client.unfocused        #111111   #191919   #888888   #000000
#client.urgent           #111111   #f00000   #ffffff   #000000
#client.placeholder      #111111   #0c0c0c   #ffffff   #000000

## Gruvbox_Theme
# client.focused          #D7992188 #D7992188 #ebdbb288 #D7992188 #D7992188
# client.focused_inactive #28282833 #28282888 #ebdbb288 #28282888 #28282888
# client.unfocused        #28282833 #28282888 #ebdbb288 #28282888 #28282888
# client.urgent           #28282833 #28282888 #fb493488 #28282888 #28282888
# client.placeholder      #28282833 #28282888 #ebdbb288 #28282888 #28282888

## Start i3bar to display a workspace bar (plus the system information i3status
## finds out, if available)
bar {
	      # status_command i3blocks
	      # status_command i3blocks -c ~/.config/i3blocks/config
        i3bar_command i3bar --transparency
        status_command i3status -c ~/.config/i3/i3status.conf
        #status_command exec ~/.config/i3/net-speed.sh
        #mode invisible
        position top
	      modifier none
        ## to hide icons
        #tray_output none
    	  colors {
       		background #00000033
        	statusline #ffffff
        	separator #666666
		      #colorclass		      #border     backgr		    text
        	focused_workspace	  #11111133	  #00000033	    #ffffff
        	active_workspace	  #11111133	  #00000033	    #ffffff
        	inactive_workspace	#11111133	  #00000033	    #888888
        	urgent_workspace	  #11111133	  #00000033	    #ffffff
        	binding_mode	      #11111133	  #00000033	    #ffffff
    	}
}
# To refresh i3 status
 set $refresh_i3status killall -SIGUSR1 i3status
## To start polybar
# exec_always --no-startup-id $HOME/.config/polybar/launch.sh

## Use pactl to adjust volume in PulseAudio & XF86keys
# bindsym XF86AudioRaiseVolume		    exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% && $refresh_i3status
# bindsym XF86AudioLowerVolume 		    exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2% && $refresh_i3status
# bindsym $mod+XF86AudioRaiseVolume 	exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
# bindsym $mod+XF86AudioLowerVolume 	exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
# bindsym XF86AudioMute 			        exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
# bindsym XF86AudioMicMute 		        exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
# bindsym XF86MonBrightnessDown		    exec --no-startup-id sudo light -U .11
# bindsym XF86MonBrightnessUp		      exec --no-startup-id sudo light -A .11
# bindsym $mod+XF86MonBrightnessDown	exec --no-startup-id sudo light -U 2
# bindsym $mod+XF86MonBrightnessUp	  exec --no-startup-id sudo light -A 2
# bindsym Shift+XF86MonBrightnessDown	exec --no-startup-id xcalib -co 75 -a
# bindsym Shift+XF86MonBrightnessUp		exec --no-startup-id xcalib -c
# bindsym $mod+F11			              exec --no-startup-id dmenumount
# bindsym $mod+F12			              exec --no-startup-id dmenuumount
# bindsym $mod+KP_Right			          exec --no-startup-id mpc next
# bindsym $mod+KP_Begin			          exec --no-startup-id mpc toggle
# bindsym $mod+KP_Left				        exec --no-startup-id mpc prev
# bindsym KP_Right			              exec --no-startup-id playerctl --player=mpv next
# bindsym KP_Begin			              exec --no-startup-id playerctl --player=mpv play-pause
# bindsym KP_Left				              exec --no-startup-id playerctl --player=mpv previous
# # bindsym KP_Right			              exec --no-startup-id playerctl next
# # bindsym KP_Begin			              exec --no-startup-id playerctl play-pause
# # bindsym KP_Left				              exec --no-startup-id playerctl previuos
# bindsym Print 				              exec --no-startup-id maim ~/bgs/bgi/"$(date '+%y%m%d-%H%M-%S').png" && sleep 0.5s && notify-send "screenshot done"
# bindsym Shift+Print 				        exec --no-startup-id maimpick
# bindsym Mod1+ctrl+1                 exec redshift -x
# bindsym Mod1+ctrl+2                 exec redshift -O 2222
# bindsym Mod1+ctrl+3                 exec redshift -O 3333
# bindsym Mod1+ctrl+4                 exec redshift -O 4444
# bindsym Mod1+ctrl+5                 exec redshift -O 5555

## exit i3 (logs you out of your X session)
bindsym $mod+Escape exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Shift+Escape	exec --no-startup-id prompt "Exit i3?" "i3 exit"

## Windows compositor
# exec_always picom --backend glx --experimental-backends --transparent-clipping
# exec_always picom --refresh-rate 30 --glx-no-rebind-pixmap --use-damage --xrender-sync-fence --backend glx --experimental-backends --transparent-clipping
# bindsym $mod+equal                   exec picom --backend glx --experimental-backends --transparent-clipping --config /home/drknss/.config/picom/picom.conf
# bindsym $mod+equal                   exec picom --refresh-rate 30 --glx-no-rebind-pixmap --use-damage --xrender-sync-fence --backend glx --experimental-backends --transparent-clipping
# bindsym $mod+Shift+equal             exec killall picom

## change focus / togle between tiling and floating windows
bindsym $mod+shift+space                 focus mode_toggle
bindsym $mod+Ctrl+space                 floating toggle

## kill focused window
bindsym $mod+q kill

## reload / restart the configuration file
bindsym $mod+w reload
bindsym $mod+Shift+w restart

 # Keybinding for filemanager
# bindsym $mod+r exec alacritty -e lf
# bindsym $mod+r exec export LF_CD=true && alacritty
# bindsym $mod+Shift+r exec sudo -E alacritty -e lf /root
# bindsym $mod+Ctrl+r exec export RANGERCD=true && urxvt

## resize window (you can also use the mouse for that)
# mode "resize" {
#         # These bindings trigger as soon as you enter the resize mode
#         # Pressing left will shrink the window’s width.
#         # Pressing right will grow the window’s width.
#         # Pressing up will shrink the window’s height.
#         # Pressing down will grow the window’s height.
#         bindsym h resize shrink width 2 px or 2 ppt
#         bindsym j resize grow height 2 px or 2 ppt
#         bindsym k resize shrink height 2 px or 2 ppt
#         bindsym l resize grow width 2 px or 2 ppt

#         # same bindings, but for the arrow keys
#         bindsym Left resize shrink width 2 px or 2 ppt
#         bindsym Down resize grow height 2 px or 2 ppt
#         bindsym Up resize shrink height 2 px or 2 ppt
#         bindsym Right resize grow width 2 px or 2 ppt

#         # back to normal: Enter or Escape or $mod+r
#         bindsym Return mode "default"
#         bindsym Escape mode "default"
#         bindsym $mod+r mode "default"
# }
##bindsym $mod+r mode "resize"
# bindsym $mod+Shift+apostrophe   move left 50 px, move up 50 px, resize grow width 100 px, resize grow height 100 px
# bindsym $mod+Ctrl+apostrophe    move left 10 px, move up 10 px, resize grow width 20 px, resize grow height 20 px
# bindsym $mod+Shift+h            move right 50 px, move down 50 px, resize shrink width 100 px, resize shrink height 100 px
# bindsym $mod+Ctrl+h             move right 10 px, move down 10 px, resize shrink width 20 px, resize shrink height 20 px
# bindsym $mod+Ctrl+u             resize shrink width 10 px
# bindsym $mod+Ctrl+i             resize grow height 10 px
# bindsym $mod+Ctrl+o             resize shrink height 10 px
# bindsym $mod+Ctrl+p             resize grow width 10 px
# bindsym $mod+Shift+u            resize shrink width 100 px
# bindsym $mod+Shift+i            resize grow height 100 px
# bindsym $mod+Shift+o            resize shrink height 100 px
# bindsym $mod+Shift+p            resize grow width 100 px

# bindsym $mod+Shift+h            move left 50 px, move up 50 px, resize grow width 100 px, resize grow height 100 px
# bindsym $mod+Ctrl+h             move left 10 px, move up 10 px, resize grow width 20 px, resize grow height 20 px
# bindsym $mod+Shift+apostrophe   move right 50 px, move down 50 px, resize shrink width 100 px, resize shrink height 100 px
# bindsym $mod+Ctrl+apostrophe    move right 10 px, move down 10 px, resize shrink width 20 px, resize shrink height 20 px
# bindsym $mod+Shift+u            resize grow width 85 px, move left 81 px
# bindsym $mod+Shift+i            resize grow height 75 px
# bindsym $mod+Shift+o            resize grow height 75 px, move up 72 px
# bindsym $mod+Shift+p            resize grow width 85 px
# bindsym $mod+Ctrl+u             resize grow width 10 px, move left 9 px
# bindsym $mod+Ctrl+i             resize grow height 10 px
# bindsym $mod+Ctrl+o             resize grow height 10 px, move up 18 px
# bindsym $mod+Ctrl+p             resize grow width 10 px
# bindsym $mod+u                  resize shrink width 85 px, move right 90 px
# bindsym $mod+i                  resize shrink height 75 px
# bindsym $mod+o                  resize shrink height 75 px, move down 90 px
# bindsym $mod+p                  resize shrink width 85 px

# bindsym $mod+Shift+apostrophe   move left 50 px, move up 25 px, resize grow width 100 px, resize grow height 50 px
# bindsym $mod+Ctrl+apostrophe    move left 10 px, move up 10 px, resize grow width 20 px, resize grow height 20 px
# bindsym $mod+Shift+h            move right 50 px, move down 25 px, resize shrink width 100 px, resize shrink height 50 px
# bindsym $mod+Ctrl+h             move right 10 px, move down 10 px, resize shrink width 20 px, resize shrink height 20 px
# bindsym $mod+apostrophe         exec xdo resize -w 800 -h 600
# bindsym $mod+h                  exec xdo resize -w 681 -h 382

# bindsym $mod+u                  exec hover left
# bindsym $mod+p                  exec hover right
# bindsym $mod+i                  exec hover down
# bindsym $mod+o                  exec hover up


# bindsym $mod+Shift+u            resize grow width 85 px, move left 85 px
# bindsym $mod+Shift+i            resize grow height 95 px
# bindsym $mod+Shift+o            resize grow height 95 px, move up 95 px
# bindsym $mod+Shift+p            resize grow width 85 px
# bindsym $mod+Ctrl+u             resize grow width 10 px, move left 10 px
# bindsym $mod+Ctrl+i             resize grow height 10 px
# bindsym $mod+Ctrl+o             resize grow height 10 px, move up 10 px
# bindsym $mod+Ctrl+p             resize grow width 10 px

# bindsym $mod+Shift+m            resize shrink width 85 px, move right 85 px
# bindsym $mod+Shift+comma        resize shrink height 95 px, move down 95 px
# bindsym $mod+Shift+period       resize shrink height 95 px
# bindsym $mod+Shift+slash        resize shrink width 85 px
# bindsym $mod+Ctrl+m             resize shrink width 10 px, move right 10 px
# bindsym $mod+Ctrl+comma         resize shrink height 10 px, move down 10 px
# bindsym $mod+Ctrl+period        resize shrink height 10 px
# bindsym $mod+Ctrl+slash         resize shrink width 10 px

## to change gaps
bindsym $mod+bracketleft          gaps inner current plus 2
bindsym $mod+Shift+bracketleft    gaps inner current minus 2
bindsym $mod+bracketright         gaps outer current plus 2
bindsym $mod+Shift+bracketright   gaps outer current minus 2

## to change container layout (stacked, tabbed, toggle split)
bindsym $mod+a                    layout tabbed
bindsym $mod+Shift+a              layout toggle split
bindsym $mod+Shift+s              layout stacking
bindsym $mod+s                    bar mode toggle

## There also is the (new) i3-dmenu-desktop which only displays applications
## shipping a .desktop file. It is a wrapper around dmenu, so you need that installed.
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+ctrl+d               exec dmenu_run
# bindsym $mod+shift+d            exec alacritty -e $(dmenu_path | dmenu)

## Borderless modes
bindsym $mod+f                    fullscreen toggle
bindsym $mod+Shift+f              border toggle 1

## focus the parent/child container
bindsym $mod+grave                focus parent
bindsym $mod+shift+grave          focus child

## change focus
#bindsym $mod+j                    focus left
#bindsym $mod+k                    focus right
#bindsym $mod+l                    exec ~/.config/i3/i3-focus-last.py --switch
#bindsym $mod+semicolon            workspace back_and_forth
#bindsym $mod+Tab                  workspace back_and_forth
# bindsym $mod+semicolon            focus left
# bindsym Mod1+semicolon            focus left
# bindsym $mod+h                    focus left
# bindsym Mod1+h                    focus left
# bindsym $mod+apostrophe           focus right
# bindsym Mod1+apostrophe           focus right
# bindsym $mod+apostrophe exec [[ -z "$I3_MSG" ]]&& { export I3_MSG="no-empty" && i3-msg focus left ; } || { export I3_MSG="" && i3-msg focus right ; }
# bindsym Mod1+apostrophe exec [[ -z "$I3_MSG" ]]&& { export I3_MSG="no-empty" && i3-msg focus left ; } || { export I3_MSG="" && i3-msg focus right ; }
#bindsym $mod+j                    exec --no-startup-id /bin/true
#bindsym $mod+k                    exec --no-startup-id /bin/true
#bindsym $mod+l                    exec skippy-xd-runner --next
#bindsym --release $mod+l          exec xdotool key j
#bindsym $mod+l                    nop
#bindsym $mod+semicolon            workspace next

## change focus with no borders
#bindsym $mod+j                   exec i3-msg border pixel 0; exec i3-msg focus left
#bindsym --release $mod+j         exec i3-msg border pixel 1
#bindsym $mod+k                   exec i3-msg border pixel 0; exec i3-msg focus right
#bindsym --release $mod+k         exec i3-msg border pixel 1
#bindsym $mod+l                   exec i3-msg border pixel 0; exec i3-focus-last.py --switch
#bindsym --release $mod+l         exec i3-msg border pixel 1
#bindsym $mod+semicolon           workspace back_and_forth

## move focused window (update:   moving to sxhkd)
# bindsym $mod+Shift+j	          move left 85 px
# bindsym $mod+Shift+k	          move down 95 px
# bindsym $mod+Shift+l	          move up 95 px
# bindsym $mod+Shift+semicolon    move right 85 px
# bindsym $mod+Ctrl+j	            move left 10 px
# bindsym $mod+Ctrl+k	            move down 10 px
# bindsym $mod+Ctrl+l	            move up 10 px
# bindsym $mod+Ctrl+semicolon     move right 10 px

## Keybinding for zystemservice
# bindsym $mod+z exec alacritty -e bashtop
# bindsym $mod+Shift+z exec alacritty -e htop --tree
# bindsym $mod+Ctrl+z exec alacritty -e gotop
# bindsym $mod+z exec urxvt -geometry 88x33 -e htop --tree
# bindsym $mod+Shift+z exec urxvt -geometry 88x33 -e bashtop

## Keybinding for i3lock
# bindsym $mod+x exec alacritty -e calcurse
# bindsym $mod+Shift+x exec i3lock

## Keybinding for chrome
# bindsym $mod+c exec google-chrome-stable --process-per-site
# bindsym $mod+Shift+c exec chromium --process-per-site
# bindsym $mod+Ctrl+c exec vimb
# bindsym $mod+b exec qutebrowser
# bindsym $mod+Shift+b exec brave

## split in vertical / horizontal orientation
bindsym $mod+v split vertical
bindsym $mod+Shift+v split horizontal

## Keybinding for media
# bindsym $mod+b exec pcmanfm
# bindsym $mod+Shift+b exec urxvt -e ncmpcpp

# Keybinding to move to corner and resize (aka maximize and minimize)(update: moving to sxhkd)
# bindsym $mod+n                    exec xdo move -x -1 -y -1; exec xdo resize -w 1366 -h 768
# bindsym $mod+Shift+n              exec xdo move -x 250 -y 15; exec xdo resize -w 1101 -h 738
# bindsym $mod+m                    exec xdo move -x 1 -y 1; exec xdo resize -w 681 -h 766
# bindsym $mod+comma                exec xdo move -x 1 -y 385; exec xdo resize -w 1364 -h 382
# bindsym $mod+period               exec xdo move -x 1 -y 1; exec xdo resize -w 1364 -h 382
# bindsym $mod+slash                exec xdo move -x 684 -y 1; exec xdo resize -w 681 -h 766


## Keybindg for press the buttons key with the keyboard
# bindsym $mod+m                    exec xdo button_press -k 2; exec sleep 0.2; exec xdo button_release -k 2
# bindsym $mod+m                    exec xdo button_press -k 4; exec sleep 0.2; exec xdo button_release -k 4
# bindsym $mod+Shift+m              exec xdo button_press -k 5; exec sleep 0.2; exec xdo button_release -k 5

## floating window bindings (needs press and release binding)
# bindsym $mod+t                    exec xdo key_press -k 133; exec xdo button_press -k 3
# bindsym $mod+y                    exec xdo key_release -k 133; exec xdo button_release -k 3
# bindsym $mod+Tab                  exec xdo button_press -k 3
# bindsym $mod+Escape               exec xdo button_release -k 3

## Keybindg for arrow keys (still working no report from bugging)
#bindsym --release $mod+l           exec --no-startup-id xdotool key --clearmodifiers Up
#bindsym --release $mod+j           exec --no-startup-id xdotool key --clearmodifiers Left
#bindsym --release $mod+k           exec --no-startup-id xdotool key --clearmodifiers Down
#bindsym --release $mod+semicolon   exec --no-startup-id xdotool key --clearmodifiers Right

## Keybindg for simulate key presses (it is still buggy but working)
#bindsym $mod+m                     exec xdo key_release -k 133;exec xdo key_release -k 58;exec xdo key_press -k 111;exec sleep 0.2;exec xdo key_release -k 111
#bindsym $mod+Shift+m               exec xdo key_release -k 133;exec xdo key_release -k 50;exec xdo key_release -k 58;exec xdo key_press -k 116;exec sleep 0.2;exec xdo key_release -k 116
#bindsym $mod+m                     exec xdo key_release -k 133;exec sleep 0.2;exec xdo key_release -k 58;exec sleep 0.2;exec xdo key_press -k 46;exec sleep 0.2;exec xdo key_release -k 46
#bindsym $mod+Shift+m               exec xdo key_release -k 133;exec sleep 0.2;exec xdo key_release -k 58;exec xdotool key KP_Enter

## Others tries trying to mimic autohotkey (backspace and ctrl+z not working)
# bindsym --release Mod1+e          exec --no-startup-id xdotool click --clearmodifiers 4
# bindsym --release Mod1+d          exec --no-startup-id xdotool click --clearmodifiers 5
# bindsym --release Mod1+s          exec --no-startup-id xdotool key --clearmodifiers ctrl+shift+Tab
# bindsym --release Mod1+f          exec --no-startup-id xdotool key --clearmodifiers ctrl+Tab
# bindsym --release Mod1+Space      exec --no-startup-id xdotool key --clearmodifiers backspace
# bindsym --release Alt_L+Alt_R     exec --no-startup-id xdotool key --clearmodifiers ctrl+z
# bindsym --release KP_Down         exec --no-startup-id xdotool key --clearmodifiers c
# bindsym --release KP_Insert       exec --no-startup-id xdotool key --clearmodifiers space






